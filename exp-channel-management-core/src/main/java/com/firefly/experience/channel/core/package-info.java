/**
 * Core service logic for the Experience Channel Management service.
 * <p>
 * Organise sub-packages by functional vertical (e.g., {@code channels/},
 * {@code providers/}). Each vertical contains a service interface and
 * implementation that delegates to downstream domain SDKs via the
 * {@code -infra} module's client factories.
 */
package com.firefly.experience.channel.core;
