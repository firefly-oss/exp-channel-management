/**
 * Infrastructure adapters for the Experience Channel Management service.
 * <p>
 * Contains {@code ClientFactory} beans and {@code @ConfigurationProperties}
 * classes that wire downstream domain SDK clients. Add one factory class
 * per downstream domain service consumed by this experience service.
 */
package com.firefly.experience.channel.infra;
